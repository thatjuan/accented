{
  "name": "accented-web",
  "version": "0.0.0",
  "private": true,
  "description": "accented.app — Cloudflare Worker fronting R2: Sparkle appcast + release enclosures.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "sync": "node scripts/publish.mjs pull",
    "publish:r2": "node scripts/publish.mjs push"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250510.0",
    "typescript": "^5.8.3",
    "wrangler": "4.96.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0"
  }
}
